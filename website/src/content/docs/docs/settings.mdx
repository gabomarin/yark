---
title: Settings
description: SteamCMD path, default folders, theme, and other app-wide preferences.
---

import { withBase } from "../../../data/site";

## SteamCMD

Set the path to the folder that contains `steamcmd.exe`. This is the most important setting — without it, install/update/verify cannot run.

<figure class="doc-shot">
  <img
    src={withBase("/screenshots/settings.png")}
    alt="Settings page with SteamCMD path, default base folder, and app preferences"
    width="1440"
    height="900"
    loading="lazy"
  />
  <figcaption>Settings — SteamCMD path, default base folder, and appearance preferences.</figcaption>
</figure>

Changing the SteamCMD path resets content-cache freshness tracking so the next install does not reuse a stale “fresh” flag from the previous home.

## Desktop shell (Windows)

- **Close window to tray** (default on) — hide to the system tray instead of quitting; tray menu has Show / Quit
- **Show notification when hiding to tray** (default on) — optional Windows toast; turn off in Settings to stop the reminder
- **Start with Windows** (default off) — open YARK at sign-in; does not auto-start dedicated servers
- **On quit with active servers** (default Ask) — Ask / Stop when quitting while servers run. Enable **Close window to tray** in Settings to keep servers and backups alive without quitting. Attach/detach is only for crash recovery or forced closes (Task Manager); there is no Leave-running quit option.

## Default base folder

Optional convenience when creating new profiles (where you tend to place installs). Each profile still stores its own absolute `installDir`.

## Theme & UI density

Appearance preferences (theme, compact vs comfortable density) apply to the desktop shell only. They do not change dedicated-server behavior.

## What stays local

Profiles, settings, events, and backup metadata live in local SQLite under the app userData folder (on Windows typically under `%APPDATA%`). There is no YARK cloud account.

See [Security & privacy](/docs/security-privacy/).

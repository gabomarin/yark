---
title: Configure SteamCMD, updates, and desktop prefs in YARK Settings
description: App-wide YARK Settings on Windows — SteamCMD path, self-update, default folders, UI density, close-to-tray, Windows notifications, and Start with Windows.
---

import { withBase } from "../../../data/site";

## SteamCMD

Set the path to the folder that contains `steamcmd.exe`. This is the most important setting — without it, install/update/verify cannot run.

<figure class="doc-shot">
  <img
    src={withBase("/screenshots/settings.png")}
    alt="Settings page with category navigation and the Open setup assistant action"
    width="1920"
    height="1080"
    loading="lazy"
  />
  <figcaption>
    Settings — app-wide preferences by category, plus the setup assistant entry point.
  </figcaption>
</figure>

Changing the SteamCMD path resets content-cache freshness tracking so the next install does not reuse a stale “fresh” flag from the previous home.

## Desktop shell (Windows)

- **Close window to tray** (default on) — hide to the system tray instead of quitting; tray menu has Show / Quit
- **Windows notifications** (default on) — toast on this PC when a dedicated server crashes, a SteamCMD install/update/verify finishes or fails, or you hide YARK to the tray. Nested **Server crash**, **SteamCMD jobs**, and **When hiding to tray** turn each category off. Click a crash toast to open that server's Events; click a SteamCMD toast to open Downloads. Focused YARK already shows in-app toasts for jobs you started, so those do not double-ping. **When hiding to tray** only appears if **Close window to tray** is on.
- **Start with Windows** (default off) — open YARK at sign-in; does not auto-start dedicated servers

When you explicitly quit while servers are active, YARK always asks you to **Stop** them or
**Cancel** the quit. Use **Close window to tray** when you want the window hidden while YARK
and its managed servers keep running. Attach/reattach is reserved for crash recovery or forced
closes; there is no normal “leave servers running and quit YARK” option.

## Default base folder

Optional convenience when creating new profiles (where you tend to place installs). Each profile still stores its own absolute `installDir`.

## UI density

**Display size** (compact vs comfortable) applies to the desktop shell only. There is no light/dark theme control — the app uses a dark shell. Density does not change dedicated-server behavior.

## Setup assistant

Use **Open setup assistant** under General to review SteamCMD, server-folder, and Windows options. With an empty fleet it also offers an optional cluster and the first create/import action. Path and Windows changes save as you go; closing the assistant does not undo them.

## Log retention

**Log retention** controls how long YARK keeps **YARK-owned** event history and SteamCMD update logs. Day fields accept 1–3650 days. ASA runtime logs under the install folder are not deleted by this policy. Configure retention and run a manual cleanup preview from Settings; see [Logs & diagnostics](/docs/logs/).

## Server auto-start summary

Settings lists profiles that opted into **Auto-start with YARK**. Turn the preference on or off on each server’s Server tab (Startup). Auto-start is separate from **Start with Windows**.

## YARK updates

**YARK updates** checks GitHub Releases for a newer desktop build (not ASA/SteamCMD). After launch, YARK quietly checks in the background; when an update is available (or a download finishes), you get an operator toast and the sidebar version uses an accent color — click either to open Settings → About. Then **Check now** → **Download** → **Restart and install**. Install is blocked while servers or critical jobs are busy. Unpackaged/dev builds can check but cannot install.

**What's new** (same notes as the public changelog) opens once after you upgrade to a version that has curated notes. Open it anytime from Settings → About, or by clicking the sidebar `vX.Y.Z` label (the update badge still opens About).

## What stays local

Profiles, settings, events, and backup metadata live in local SQLite under the app userData folder (on Windows typically under `%APPDATA%`). There is no YARK cloud account. Server/admin credentials are local too; review the current storage and backup boundary before sharing app data or INI files.

See [Security & privacy](/docs/security-privacy/).

## Profile database recovery

The profile SQLite file is `yark-server-manager.db` under App data (open that folder from Settings → About). On open, YARK waits briefly for a lock so a short contention does not fail boot immediately.

If the database cannot open or migrate, boot shows a recovery dialog instead of a blank window:

- **Open folder** — inspect or copy the DB (and related WAL/SHM files) from App data
- **Quit** — exit without changing files
- **Start empty…** — YARK does not repair the file; it renames the broken DB + WAL/SHM as `*.corrupt.<timestamp>` and continues with a blank database

ASA installs on disk are not deleted. You will need to re-add profiles if you start empty.

---
title: Install YARK and run your first ASA dedicated server
description: "Install YARK on Windows, point SteamCMD, create a server profile, and start your first local ARK: Survival Ascended dedicated server."
---

import { Aside, Steps } from "@astrojs/starlight/components";
import { withBase } from "../../../data/site";

<Aside type="caution" title="Public prerelease">
  Download only from [GitHub Releases](https://github.com/gabomarin/yark/releases) or the site
  download button, then verify the installer hash below.
</Aside>

<Steps>

1. **Install YARK**

   Download the current Windows installer and run it. After install, open **YARK server manager**.

   Before running the installer, follow [Verify your download](#verify-your-download)
   below and compare it with the matching official release asset.

2. **Point SteamCMD**

   Install [SteamCMD](https://developer.valvesoftware.com/wiki/SteamCMD) somewhere writable.
   In YARK → **Settings**, set the SteamCMD path (folder that contains `steamcmd.exe`).

   <figure class="doc-shot">
     <img
       src={withBase("/screenshots/settings.png")}
       alt="Settings page with SteamCMD path, default base folder, and app preferences"
       width="1440"
       height="900"
       loading="eager"
     />
     <figcaption>Settings — set the folder that contains steamcmd.exe.</figcaption>
   </figure>

   One SteamCMD home is shared across profiles. Next to it, YARK maintains:

   | Cache | Role |
   | --- | --- |
   | `steamapps/depotcache` | Compressed Steam downloads |
   | `asa_content_cache` | Shared expanded ASA files copied into each server |

3. **Create or import a server profile**

   **New server** creates a profile with:

   - **Install path** — absolute Windows path (`C:\ARK\MyServer` or UNC)
   - **Map** + **session name** — **New server** is official maps only. After create, CurseForge map packs need the pack on **Mods** (enabled) plus the launch token under Map (**Map mods** or **Custom…**)
   - **Ports** — unique game / query / RCON within the profile and across other profiles (the form shows live **port conflicts** vs the fleet)
   - **Admin password** — at least 4 characters
   - **Cluster** — **None**, or pick an **existing fleet cluster** (fills Cluster ID + shared directory). If you have no clusters yet, use **Create a cluster first…** from the form, or create one later on the Clusters page.

   You can leave a new profile **disabled** until files are ready; **Enable** does not require a Ready install. **Start**, **Restart**, and auto-start still do. For a first custom map, see [Mods → Maps / custom worlds](/docs/mods/#maps--custom-worlds).

   Already have ASA files on disk? Use **Import install** (Overview / workspace **New server ▾**) to adopt that folder as a profile without rewriting INI until you Start (or Install/Verify). Select the folder that **contains** `ShooterGame` (not `ShooterGame\Binaries\Win64`). When Import reports **Ready**, skip step 4 and go to **Start** (or take a backup first). For an **Incomplete** tree, check **Import anyway — Install/Verify before Start**, then run Install/Verify before Start. Empty folders are not importable — use **New server** on that path, then Install.

4. **Install dedicated files** (new installs only)

   From the server actions, run the files **install / update**. First sync can take a long time and needs free disk for the shared cache **and** the per-server copy. Worlds under `ShooterGame\Saved` are not overwritten by the cache sync. Skip this step when you used **Import install** and the profile is already **Ready**; run it after an incomplete Import opt-in.

5. **Start the server**

   Press **Start**. Status stays **starting** until RCON answers on localhost — that means “accepting players”, not merely “process spawned”. Use **Logs → Runtime / Events** if it stalls.

   <figure class="doc-shot">
     <img
       src={withBase("/screenshots/overview.png")}
       alt="Servers overview listing profiles with status and actions"
       width="1440"
       height="900"
       loading="lazy"
     />
     <figcaption>Overview — install/update and Start live on each server card.</figcaption>
   </figure>

6. **Take a backup before experimenting**

   Open the workspace **Backups** tab and create at least a world backup. Prefer **safe update** / **restart** later so critical-path snapshots run for you.

</Steps>

Tip: **Ctrl+K** opens Quick jump to pages and servers. Structured ASA launch flags live on the
workspace **Launch** tab (see [Start, stop, restart](/docs/lifecycle/#launch-tab)).

## Verify your download

Use the exact installer name for the version you downloaded:

```powershell
Get-FileHash ".\YARK-server-manager-Setup-VERSION.exe" -Algorithm SHA256
```

Open [YARK Releases](https://github.com/gabomarin/yark/releases), select the matching version,
and compare the complete SHA-256 value. Do not install the file if the values differ or if it
came from a mirror you do not trust.

## Next reads

- [Profiles & ports](/docs/profiles-and-ports/) — validation rules and conflicts
- [Start, stop, restart](/docs/lifecycle/) — what each action does
- [Updates & SteamCMD](/docs/updates-and-steamcmd/) — patching without losing worlds
- [Backups](/docs/backups/) — schedules and restore rules

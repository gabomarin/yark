/** Responsive WebP widths for `/screenshots/*.png` (1440×900 capture). */
export const SHOT_WIDTHS = [640, 960, 1280, 1440];

/** UI chrome needs a higher setting than photos; q72 looked blocky in the home slideshow. */
export const SHOT_WEBP_QUALITY = 88;

{
  "name": "yark-curseforge-proxy",
  "private": true,
  "version": "0.1.0",
  "description": "Cloudflare Worker proxy for CurseForge Core API (ASA). Holds CURSEFORGE_API_KEY server-side.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260722.1",
    "typescript": "^5.8.3",
    "wrangler": "^4.114.0"
  }
}

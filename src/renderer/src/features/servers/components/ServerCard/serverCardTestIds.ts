/**
 * Stable `data-*` hooks for ServerCard / Overview (#457). Attribute **values**
 * must not change without coordinating E2E docs/scripts.
 */

export const SERVER_CARD_ATTR = "data-server-card";
export const SERVER_CARD_SELECTOR = "[data-server-card]";

export const META_GRID_ATTR = "data-meta-grid";
export const META_GRID_SELECTOR = "[data-meta-grid]";
